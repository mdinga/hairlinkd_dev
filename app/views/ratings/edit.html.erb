<div class = "container">

  <div>
    <%= link_to("<< Back to Stylist", show_stylists_client_path(@stylist)) %>
  </div>


  <div>

  <%= form_for(@rating) do |f| %>

    <%= error_messages_for(f.object) %>

   <%= render(:partial => 'ratings_form', :locals => {:f => f}) %>

    <% end %>
  </div>
</div>
