<div class = "m-1">
  <%= button_to("<< Back to Profile", client_path(current_user), method: :get, class: "btn btn-sm") %>
</div>

<h3 class = 'text-center m-2' style = "text-decoration:underline;"> Your Hairstyle Requests</h3>
<hr>

<% if @requests.count > 0 %>
  <% @requests.each do |request| %>
  <%= render(:partial => 'index_requests', :html => {:multiport => true}, :locals => {:request => request})%>
    <hr>
  <% end %>
<% else %>
  <p style = "font-size:120%"> Describle the hairstyle that you require. We will make the stylists in your area aware of your request and they can communicate directly with you. All your requests will be kepts here.</p>
    <div class = "m-3 text-center">
      <%= button_to("Create Request", new_request_path, method: :get, class: "btn btn-sm") %>
    </div>
<% end %>
