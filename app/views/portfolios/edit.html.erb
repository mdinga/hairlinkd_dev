<div class = "container">

  <div class = "m-3">
    <%= button_to("<< Back to Profile", stylist_path(current_user), method: :get, class: "btn btn-sm") %>
  </div>

    <h4 class = 'text-center'>Update Item</h4>

  <%= form_for(@portfolio) do |p| %>

    <%= render(:partial => 'portfolio_form', :locals => {:p => p}) %>


    <div class = "text-center">
      <%= p.submit "Update Item", class: "btn btn-sm" %>
    </div>

  <% end %>
</div>
